1️⃣ What is the difference between var, let, and const?

To diclare variables in js we use `var`,let and const.Although we don't use var anymore because its have some problem.
We use `const` when we don't want to change its value.
And we use `let` when we need the value that can be changed. 


2️⃣ What is the spread operator (...)?

Spread Operator Separate all the elements from an object or an array. After separating it, make it into individual elements. 


3️⃣ What is the difference between map(), filter(), and forEach()?

The difference between these three is:
The map makes a new array with a changed value.
The filter makes an array with a specific value
The forEach just doing something with each item without creating a new one.

4️⃣ What is an arrow function?

Array function also does the same thing as a normal function does, but it's much easier to write as code and it's much easier to read, so it's much more understandable and it's use the => arrow like this.


5️⃣ What are template literals?

Template literals are simple way to Create strings in JavaScript, and it uses   `backticks` instead of "quotes". I can also use variables directly inside the strings without complicated concatenation. It's much easier and cleaner. 
